{
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "echo 'This repo uses FORGE governance. Use the forge skill explicitly. Read docs/forge/AI.md, docs/forge/CONTEXT.md if present, the compact task index, and one selected task. Do not load all docs/forge files at startup. If forge is unavailable, stop and tell the user FORGE must be installed before continuing.'"
      }
    ]
  }
}
