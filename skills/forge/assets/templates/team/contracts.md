# Team Contracts

- Use `contract_files` for shared API, schema, generated client, migration, or message-format artifacts.
- Update contract artifacts in the same task that changes behavior across the boundary.
- If another active task owns the same contract file, stop and resequence.
